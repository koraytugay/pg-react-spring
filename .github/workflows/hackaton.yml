name: Use Hello World Action

on: [push]

jobs:
  run-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Hello World Action
        uses: koraytugay/hackaton@v1
